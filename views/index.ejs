<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Management Dashboard - Dark</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Ensuring the background remains dark even if content is short */
        body {
            background-color: #1a202c; 
        }
    </style>
</head>
<body class="bg-gray-900 text-gray-100">

    <div class="container mx-auto p-4 sm:p-6 lg:p-8">
        
        <div class="flex flex-col md:flex-row justify-between items-center mb-6 border-b border-gray-700 pb-4">
            <h1 class="text-3xl font-bold text-white">
                👤 Registered Users List
            </h1>
            
            <a href="/create" class="mt-4 md:mt-0 bg-indigo-600 hover:bg-indigo-500 text-white font-semibold py-2 px-6 rounded-lg transition duration-200 ease-in-out shadow-lg">
                + Create New User
            </a>
        </div>

        <div class="bg-gray-800 shadow-2xl rounded-lg overflow-hidden">
            
            <div class="hidden md:grid grid-cols-6 gap-4 p-4 border-b border-gray-700 bg-gray-700 text-sm font-semibold text-gray-300 uppercase tracking-wider">
                <div class="col-span-1">Image</div>
                <div class="col-span-2">Full Name</div>
                <div class="col-span-2">Database ID</div>
                <div class="col-span-1 text-right">Actions</div>
            </div>

            <% if (users && users.length > 0) { %>
                <% users.forEach((user, i) => { %>
                    <div class="grid grid-cols-2 md:grid-cols-6 gap-4 p-4 hover:bg-gray-700/50 transition duration-150 ease-in-out 
                        <%= (i < users.length - 1) ? 'border-b border-gray-700' : '' %>">
                        
                        <div class="flex items-center space-x-3 col-span-2 md:col-span-3">
                            <div class="flex-shrink-0 h-10 w-10">
                                <img class="h-10 w-10 rounded-full object-cover border-2 border-indigo-500" 
                                     src="<%= user.image %>" 
                                     alt="<%= user.FirstName %> <%= user.LastName %> Avatar">
                            </div>
                            <div>
                                <div class="text-sm font-medium text-white">
                                    <%= user.FirstName %> <%= user.LastName %>
                                </div>
                                <div class="text-xs text-gray-400 md:hidden">Full Name</div>
                            </div>
                        </div>

                        <div class="hidden md:flex items-center text-sm text-gray-400 col-span-2">
                            <%= user._id %>
                        </div>

                        <div class="flex items-center justify-end md:justify-end text-sm font-medium col-span-2 md:col-span-1">
                            <a href="/user/<%= user._id %>" class="text-indigo-400 hover:text-indigo-300 mr-4">View</a>
                            
                            <form action="/delete/<%= user._id %>" method="POST" onsubmit="return confirm('Are you sure you want to delete this user?');">
                                <button type="submit" class="text-red-400 hover:text-red-300 font-medium bg-transparent border-none p-0 cursor-pointer">
                                    Delete
                                </button>
                            </form>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="p-8 text-center text-gray-400">
                    <svg class="mx-auto h-12 w-12 text-gray-500" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354l.352.793a4.02 4.02 0 011.666 1.666l.793.352-1.354 1.354a6.02 6.02 0 00-4.008 0l-1.354-1.354.793-.352a4.02 4.02 0 011.666-1.666l.352-.793zM21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                    </svg>
                    <h3 class="mt-2 text-sm font-medium text-white">No Users</h3>
                    <p class="mt-1 text-sm text-gray-400">
                      Get started by adding a new user or checking your database connection.
                    </p>
                </div>
            <% } %>
        </div>
        
    </div>

</body>
</html>