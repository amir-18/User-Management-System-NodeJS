<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile: <%= user.FirstName %> <%= user.LastName %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            background-color: #1a202c; /* bg-gray-900 */
        }
    </style>
</head>
<body class="bg-gray-900 text-gray-100">

    <div class="container mx-auto p-4 sm:p-6 lg:p-10 max-w-2xl">
        
        <div class="bg-gray-800 shadow-2xl rounded-xl overflow-hidden">
            
            <div class="p-6 sm:p-8 border-b border-gray-700 flex items-center justify-between">
                <h1 class="text-3xl font-extrabold text-white">
                    User Profile
                </h1>
                
                <div>
                    <a href="/edit/<%= user._id %>" 
                       class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-indigo-100 bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 focus:ring-offset-gray-800 transition duration-150">
                        <svg class="w-5 h-5 mr-2"   fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path></svg>
                        Edit
                    </a>
                </div>
            </div>

            <div class="p-6 sm:p-8">
                
                <div class="flex flex-col sm:flex-row items-center mb-8">
                    <img class="w-24 h-24 rounded-full object-cover border-4 border-indigo-500 shadow-lg mb-4 sm:mb-0 sm:mr-6" 
                         src="<%= user.image %>" 
                         alt="<%= user.FirstName %> <%= user.LastName %> Avatar">
                    <div class="text-center sm:text-left">
                        <h2 class="text-4xl font-bold text-white"><%= user.FirstName %> <%= user.LastName %></h2>
                        <p class="text-lg text-indigo-400 mt-1">Registered User</p>
                    </div>
                </div>
                
                <dl class="divide-y divide-gray-700">
                    
                    <div class="py-4 sm:py-5 sm:grid sm:grid-cols-3 sm:gap-4">
                        <dt class="text-sm font-medium text-gray-400">Full Name</dt>
                        <dd class="mt-1 text-sm text-white sm:mt-0 sm:col-span-2">
                            <%= user.FirstName %> <%= user.LastName %>
                        </dd>
                    </div>

                    <div class="py-4 sm:py-5 sm:grid sm:grid-cols-3 sm:gap-4">
                        <dt class="text-sm font-medium text-gray-400">First Name</dt>
                        <dd class="mt-1 text-sm text-white sm:mt-0 sm:col-span-2">
                            <%= user.FirstName %>
                        </dd>
                    </div>

                    <div class="py-4 sm:py-5 sm:grid sm:grid-cols-3 sm:gap-4">
                        <dt class="text-sm font-medium text-gray-400">Last Name</dt>
                        <dd class="mt-1 text-sm text-white sm:mt-0 sm:col-span-2">
                            <%= user.LastName %>
                        </dd>
                    </div>

                    <div class="py-4 sm:py-5 sm:grid sm:grid-cols-3 sm:gap-4">
                        <dt class="text-sm font-medium text-gray-400">Database ID</dt>
                        <dd class="mt-1 text-sm font-mono text-gray-300 sm:mt-0 sm:col-span-2 break-all">
                            <%= user._id %>
                        </dd>
                    </div>
                    
                </dl>
            </div>
            
            <div class="px-6 py-4 bg-gray-700/50 text-center">
                <a href="/read" class="text-indigo-400 hover:text-indigo-300 text-sm font-medium">
                    ← Back to User List
                </a>
            </div>

        </div>
        
    </div>

</body>
</html>